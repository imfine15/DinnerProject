listCount=SELECT COUNT(*) FROM ENTERPRISE WHERE UPLOAD_APPROVAL='N'
selectEntList=SELECT RNUM, ENP_NAME, ENP_TYPE, UPLOAD_DATE, UPLOAD_APPROVAL FROM(SELECT ROWNUM RNUM, ENP_NAME, ENP_TYPE, UPLOAD_DATE, UPLOAD_APPROVAL FROM(SELECT E.ENP_NAME, E.ENP_TYPE, F.UPLOAD_DATE, E.UPLOAD_APPROVAL FROM ENTERPRISE E JOIN ENP_FILE F ON (E.ENP_NO = F.ENP_NO) WHERE E.UPLOAD_APPROVAL = 'N' ORDER BY E.ENP_NO DESC)) WHERE RNUM BETWEEN ? AND ?