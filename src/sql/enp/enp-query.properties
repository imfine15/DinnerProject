insertPartnerEnp1=INSERT INTO ENTERPRISE(ENP_NO, ENP_NAME, ENP_PHONE, ENP_ADDRESS, ENP_HOUR, ENP_TYPE, ENP_STATUS, ENP_PARTNER_TYPE,PRICE_RANGE,CLOSED_DAY,WEBSITE,INTRODUCE,PARKING_POSSIBLE) VALUES('ENP'||SEQ_ENP_NO.NEXTVAL,?,?,?,?,?,'Y','PARTNER',?,?,?,?,?)
insertEnterprise=INSERT INTO ENTERPRISE VALUES('ENP'||SEQ_ENP_NO.NEXTVAL,?,?,?,?,?,'N','NORMAL',?,?,?,?,?,?,'N')
insertPartnerEnp2=INSERT INTO PARTNER_ENP(ENP_NO, ENP_REGISTER_NO, PARTNER_CODE, PENALTY_COUNT, PARTNER_ID , PARTNER_PWD, PARTNER_EMAIL, PARTNER_NAME, ACCOUNT_HOLDER, BANK, BANK_ACCOUNT, DEPOSIT_LOWER_LIMIT, DEPOSIT_HIGHER_LIMIT, SIGNUP_APPROVAL, JUMIN_NO, ENP_LICENCE) VALUES('ENP'||SEQ_ENP_NO.CURRVAL,?,'BASIC',0,?,?,?,?,?,?,?,?,?,'N',?,?)
selectCurrval=SELECT SEQ_ENP_NO.CURRVAL FROM DUAL
insertAttachment=INSERT INTO ENP_FILE VALUES('ENTF'||SEQ_ENP_FILE_NO.NEXTVAL,?,?,?,SYSDATE,?)
insertMenu=INSERT INTO MENU VALUES('MENU'||SEQ_MENU_NO.NEXTVAL,?,?,?)
loginCheck=SELECT * FROM PARTNER_ENP JOIN ENTERPRISE USING (ENP_NO) WHERE PARTNER_ID = ? AND PARTNER_PWD = ?
selectList=SELECT RNUM, RESERVATION_NO, RESERVATION_DATE, TO_CHAR(RESERVATION_DATE,'HH24:MI'),MEMBER_NO, ENP_NO, CALC_NO, REQUEST_MEMO, POINT_AMMOUNT, PEOPLE FROM(SELECT ROWNUM RNUM, RESERVATION_NO, RESERVATION_DATE, MEMBER_NO, ENP_NO, CALC_NO, REQUEST_MEMO, POINT_AMMOUNT, PEOPLE FROM (SELECT RESERVATION_NO, RESERVATION_DATE, MEMBER_NO, ENP_NO, CALC_NO, REQUEST_MEMO, POINT_AMMOUNT, PEOPLE FROM RESERVATION WHERE ENP_NO = ? ORDER BY RESERVATION_NO DESC)) WHERE RNUM BETWEEN ? AND ?
listCount=SELECT COUNT(*) FROM RESERVATION
selectCRMemId=SELECT MEMBER_NO FROM MEMBER JOIN RESERVATION_HISTORY USING(MEMBER_NO) JOIN RESERVATION USING(MEMBER_NO) WHERE ENP_NO = ?
selectCRModalList=SELECT ROWNUM, MEMBER_NICKNAME, RESERVATION_DATE FROM RESERVATION_HISTORY JOIN RESERVATION_VISIT USING(MEMBER_NO) JOIN MEMBER USING(MEMBER_NO) WHERE MEMBER_NO = ? ORDER BY RESERVATION_DATE ASC
selectCRCount=SELECT COUNT(*) FROM RESERVATION_HISTORY JOIN RESERVATION_STATUS USING(STATUS_CODE) JOIN RESERVATION USING(RESERVATION_NO) WHERE ENP_NO = ? AND STATUS_CODE = ?