insertPartnerEnp1=INSERT INTO ENTERPRISE VALUES('ENP'||SEQ_ENP_NO.NEXTVAL,?,?,?,?,?,'N','PARTNER',?,?,?,?,?,?, DEFAULT, DEFAULT)
insertEnterprise=INSERT INTO ENTERPRISE VALUES('ENP'||SEQ_ENP_NO.NEXTVAL,?,?,?,?,?,'N','NORMAL',?,?,?,?,?,?,'N',0)
insertPartnerEnp2=INSERT INTO PARTNER_ENP(ENP_NO, ENP_REGISTER_NO, PARTNER_CODE, PENALTY_COUNT, PARTNER_ID , PARTNER_PWD, PARTNER_EMAIL, PARTNER_NAME, ACCOUNT_HOLDER, BANK, BANK_ACCOUNT, DEPOSIT_LOWER_LIMIT, DEPOSIT_HIGHER_LIMIT, SIGNUP_APPROVAL, JUMIN_NO, ENP_LICENCE) VALUES('ENP'||SEQ_ENP_NO.CURRVAL,?,'BASIC',0,?,?,?,?,?,?,?,?,?,'N',?,?)
selectCurrval=SELECT SEQ_ENP_NO.CURRVAL FROM DUAL

insertAttachment=INSERT INTO ENP_FILE VALUES('ENTF'||SEQ_ENP_FILE_NO.NEXTVAL,?,?,?,SYSDATE,?)
insertMenu=INSERT INTO MENU VALUES('MENU'||SEQ_MENU_NO.NEXTVAL,?,?,?)

loginCheck=SELECT * FROM PARTNER_ENP JOIN ENTERPRISE USING (ENP_NO) WHERE PARTNER_ID = ? AND PARTNER_PWD = ?
selectList=SELECT RNUM, RESERVATION_NO, RESERVATION_DATE, TO_CHAR(RESERVATION_DATE,'HH24:MI') ,MEMBER_NO, ENP_NO, CALC_NO, REQUEST_MEMO, POINT_AMMOUNT, PEOPLE  FROM( SELECT ROWNUM AS RNUM, R.RESERVATION_NO, R.RESERVATION_DATE, R.MEMBER_NO, R.ENP_NO, R.CALC_NO , REQUEST_MEMO, POINT_AMMOUNT, PEOPLE FROM RESERVATION R JOIN RESERVATION_HISTORY H ON(R.RESERVATION_NO = H.RESERVATION_NO) WHERE H.ROWID IN (SELECT MAX(ROWID) FROM RESERVATION_HISTORY GROUP BY RESERVATION_NO ) AND ENP_NO = ? AND STATUS_CODE = 'RSC1') WHERE RNUM BETWEEN ? AND ? ORDER BY TO_NUMBER(SUBSTR(RESERVATION_NO,4)) 
listCount=SELECT COUNT(*)  FROM( SELECT ROWNUM AS RNUM, R.RESERVATION_NO, R.RESERVATION_DATE, R.MEMBER_NO, R.ENP_NO, R.CALC_NO , REQUEST_MEMO, POINT_AMMOUNT, PEOPLE FROM RESERVATION R JOIN RESERVATION_HISTORY H ON(R.RESERVATION_NO = H.RESERVATION_NO) WHERE H.ROWID IN (SELECT MAX(ROWID) FROM RESERVATION_HISTORY GROUP BY RESERVATION_NO ) AND ENP_NO = ? AND STATUS_CODE = 'RSC1')

selectCRMemId=SELECT MEMBER_NO FROM MEMBER JOIN RESERVATION_HISTORY USING(MEMBER_NO) JOIN RESERVATION USING(MEMBER_NO) WHERE ENP_NO = ?
selectCRModalList=SELECT ROWNUM, MEMBER_NICKNAME, RESERVATION_DATE , STATUS_CODE ,SUBSTR(RESERVATION_NO,4) FROM( SELECT ROWNUM, MEMBER_NICKNAME, r.RESERVATION_DATE , STATUS_CODE, R.RESERVATION_NO FROM RESERVATION R JOIN RESERVATION_HISTORY H ON(R.RESERVATION_NO = H.RESERVATION_NO) JOIN MEMBER M ON (R.MEMBER_NO = M.MEMBER_NO) WHERE H.ROWID IN (SELECT MAX(ROWID) FROM RESERVATION_HISTORY GROUP BY RESERVATION_NO ) AND ENP_NO = ? AND STATUS_CODE = 'RSC1' ) ORDER BY SUBSTR(RESERVATION_NO,4)
selectCRCount=SELECT COUNT(*) FROM RESERVATION_HISTORY JOIN RESERVATION_STATUS USING(STATUS_CODE) JOIN RESERVATION R USING(RESERVATION_NO) WHERE ENP_NO = ? AND STATUS_CODE = ? AND R.MEMBER_NO = ?
selectEnp=SELECT * FROM ENTERPRISE E JOIN ENP_FILE F ON(F.ENP_NO = E.ENP_NO) JOIN MENU M ON(M.ENP_NO = E.ENP_NO) WHERE E.ENP_NO = ?

selectCmMemberVal=SELECT  ENP_NO , REVIEW_NO , R.REVIEW_CONTENT , R.REVIEW_TYPE , R.VISIT_DATE , R.REVIEW_DATE , R.AVERAGE_RATING , RR.REPLY_CONTENT , MEMBER_NO , M.MEMBER_NAME , E.ENP_NAME , M.MEMBER_NICKNAME , MF.FILE_NO , MF.ORIGIN_NAME , MF.CHANGE_NAME , MF.FILE_PATH FROM REVIEW R JOIN REVIEW_REPLY RR USING (REVIEW_NO) JOIN MEMBER M USING (MEMBER_NO) JOIN ENTERPRISE E ON (R.ENP_NO = E.ENP_NO) JOIN MEMBER_FILE MF USING (MEMBER_NO) JOIN REVIEW_FILE RF USING(REVIEW_NO) WHERE AVERAGE_RATING IS NOT NULL AND REPLY_STATUS = 'N' AND ENP_NO = ?
insertComment=INSERT INTO REVIEW_REPLY (REPLY_NO,REVIEW_NO,REPLY_CONTENT,ENP_NO) VALUES ('REP'||SEQ_REVIEW_REPLY_NO.NEXTVAL,?,?,?)
getEnpFile=SELECT FILE_PATH FROM ENP_FILE WHERE ENP_NO = ?
selectCmFile=SELECT FILE_PATH, CHANGE_NAME FROM REVIEW_FILE JOIN REVIEW USING(REVIEW_NO) WHERE ENP_NO = ?
updateReplyStatus=UPDATE REVIEW SET REPLY_STATUS = 'Y' WHERE REVIEW_NO = ?

selectCurrentList=SELECT * FROM( SELECT * FROM RESERVATION_HISTORY H WHERE RESERVATION_NO = ?  ORDER BY ROWNUM DESC ) JOIN RESERVATION R USING(RESERVATION_NO) JOIN ENTERPRISE E USING(ENP_NO) WHERE ROWNUM = 1

selectRDList=SELECT RNUM, RESERVATION_NO, RESERVATION_DATE, TO_CHAR(RESERVATION_DATE,'HH24:MI') ,MEMBER_NO, ENP_NO, CALC_NO, REQUEST_MEMO, POINT_AMMOUNT, PEOPLE ,  SUBSTR(RESERVATION_NO,4) FROM( SELECT ROWNUM RNUM, R.RESERVATION_NO, R.RESERVATION_DATE, R.MEMBER_NO, R.ENP_NO, R.CALC_NO, R.REQUEST_MEMO, R.POINT_AMMOUNT, R.PEOPLE, M.MEMBER_NICKNAME FROM RESERVATION R JOIN RESERVATION_HISTORY H ON(R.RESERVATION_NO = H.RESERVATION_NO) JOIN MEMBER M ON (R.MEMBER_NO = M.MEMBER_NO) WHERE H.ROWID IN (SELECT MAX(ROWID) FROM RESERVATION_HISTORY GROUP BY RESERVATION_NO ) AND ENP_NO = ? AND STATUS_CODE = 'RSC2' ) WHERE ROWNUM BETWEEN ? AND ? AND TO_CHAR(RESERVATION_DATE,'RRMMDD') = TO_CHAR(TO_DATE(?),'RRMMDD') ORDER BY TO_NUMBER(SUBSTR(RESERVATION_NO,4))
selectRDModalList=SELECT ROWNUM, MEMBER_NICKNAME, RESERVATION_DATE , STATUS_CODE ,MEMBER_PHONE, SUBSTR(RESERVATION_NO,4) FROM( SELECT ROWNUM, MEMBER_NICKNAME, r.RESERVATION_DATE , STATUS_CODE, MEMBER_PHONE, R.RESERVATION_NO FROM RESERVATION R JOIN RESERVATION_HISTORY H ON(R.RESERVATION_NO = H.RESERVATION_NO) JOIN MEMBER M ON (R.MEMBER_NO = M.MEMBER_NO) WHERE H.ROWID IN (SELECT MAX(ROWID) FROM RESERVATION_HISTORY GROUP BY RESERVATION_NO ) AND ENP_NO = ? AND STATUS_CODE = 'RSC2' ) WHERE TO_CHAR(RESERVATION_DATE,'RRMMDD') = TO_CHAR(TO_DATE(?),'RRMMDD') ORDER BY TO_NUMBER(SUBSTR(RESERVATION_NO,4))

checkId=SELECT PARTNER_ID FROM PARTNER_ENP WHERE PARTNER_ID = ?

uploadEnp=UPDATE ENTERPRISE SET UPLOAD_APPROVAL='Y' WHERE ENP_NO=?
sdListCount=SELECT COUNT(*) FROM CONTRACT_HISTORY WHERE ENP_NO = ?
selectSDList=SELECT ROWNUM, PRODUCT_NAME, PARTNER_PRICE, CONTRACT_START_DATE, CONTRACT_END_DATE FROM(SELECT PRODUCT_NAME, PARTNER_PRICE, CONTRACT_START_DATE, CONTRACT_END_DATE FROM PARTNER_ENP JOIN PARTNER USING(PARTNER_CODE) JOIN CONTRACT_HISTORY USING(ENP_NO) WHERE ENP_NO = ? AND ROWNUM BETWEEN ? AND ? ORDER BY CONTRACT_END_DATE DESC)

updateCrList=INSERT INTO RESERVATION_HISTORY(RESERVATION_HISTORY_NO,RESERVATION_NO,HEADCOUNT,STATUS_CODE,RESERVATION_DATE,DEPOSIT,MEMBER_NO) ( SELECT 'RESH'||SEQ_RESERVATION_HISTORY_NO.NEXTVAL,RESERVATION_NO,HEADCOUNT,'RSC2',RESERVATION_DATE,DEPOSIT,MEMBER_NO FROM (SELECT ROWNUM,RESERVATION_HISTORY_NO,RESERVATION_NO,HEADCOUNT,STATUS_CODE,RESERVATION_DATE,DEPOSIT,MEMBER_NO FROM RESERVATION_HISTORY WHERE RESERVATION_NO = ? AND ROWNUM = 1 ORDER BY RESERVATION_HISTORY_NO DESC) )
deleteCrList=INSERT INTO RESERVATION_HISTORY(RESERVATION_HISTORY_NO,RESERVATION_NO,HEADCOUNT,STATUS_CODE,RESERVATION_DATE,DEPOSIT,MEMBER_NO) ( SELECT 'RESH'||SEQ_RESERVATION_HISTORY_NO.NEXTVAL,RESERVATION_NO,HEADCOUNT,'RSC4',RESERVATION_DATE,DEPOSIT,MEMBER_NO FROM (SELECT ROWNUM,RESERVATION_HISTORY_NO,RESERVATION_NO,HEADCOUNT,STATUS_CODE,RESERVATION_DATE,DEPOSIT,MEMBER_NO FROM RESERVATION_HISTORY WHERE RESERVATION_NO = ? AND ROWNUM = 1 ORDER BY RESERVATION_HISTORY_NO DESC) )
selectCrInfoModalList=SELECT RNUM, MEMBER_NAME, MEMBER_EMAIL, MEMBER_PHONE, REQUEST_MEMO, RESERVATION_NO FROM( SELECT ROWNUM RNUM, R.RESERVATION_NO,MEMBER_NAME, MEMBER_EMAIL, MEMBER_PHONE, REQUEST_MEMO , R.RESERVATION_DATE FROM RESERVATION R JOIN RESERVATION_HISTORY H ON(R.RESERVATION_NO = H.RESERVATION_NO) JOIN MEMBER M ON (R.MEMBER_NO = M.MEMBER_NO) WHERE H.ROWID IN (SELECT MAX(ROWID) FROM RESERVATION_HISTORY GROUP BY RESERVATION_NO ) AND ENP_NO = ? AND STATUS_CODE = 'RSC1' ) ORDER BY TO_NUMBER(SUBSTR(RESERVATION_NO,4))